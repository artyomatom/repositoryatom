# Лабораторная работа N 3. Вариант 11

## Задания

### Задача 1

Найдите количество пятизначных восьмеричных чисел, в которых все цифры различны и никакие две чётные или нечётные не стоят рядом.

### Задача 2

Значение выражения $81^{17} + 3^{24} - 45$ записали в системе счисления с основанием 9. Сколько цифр 8 содержится в этой записи?

### Задача 3

Назовём нетривиальным делителем натурального числа его делитель, не равный единице и самому числу. Найдите все натуральные числа, принадлежащие отрезку $[123456789;\, 223456789]$ и имеющие ровно три нетривиальных делителя. Для каждого найденного числа выведите его наибольший нетривиальный делитель. Ответы расположите в порядке возрастания.

---

## Описание проделанной работы

### Задача 1

Используя `itertools.permutations`, генерируем все перестановки из 5 различных цифр восьмеричной системы (0–7). Отсекаем числа, начинающиеся с нуля. Для каждого кода проверяем, что ни две чётные ({0, 2, 4, 6}), ни две нечётные ({1, 3, 5, 7}) цифры не стоят рядом.

```python
from itertools import permutations

def solve_ex1():
    digits = [0, 1, 2, 3, 4, 5, 6, 7]
    even_digits = {0, 2, 4, 6}
    odd_digits  = {1, 3, 5, 7}
    count = 0
    for code in permutations(digits, 5):
        if code[0] == 0:
            continue
        flag = True
        for i in range(len(code) - 1):
            digit, next_d = code[i], code[i + 1]
            if digit in even_digits and next_d in even_digits:
                flag = False
                break
            if digit in odd_digits and next_d in odd_digits:
                flag = False
                break
        if flag:
            count += 1
    return count
```

### Задача 2

Вычисляем значение выражения средствами Python (BigInteger), затем переводим его в девятичную систему счисления последовательным делением на 9. Подсчитываем количество вхождений цифры `8` в полученной строке.

```python
def solve_ex2():
    expression = (81**17) + (3**24) - 45

    def translator(num):
        res = ''
        while num > 0:
            res = str(num % 9) + res
            num //= 9
        return res

    new_expr = translator(expression)
    return new_expr.count("8")
```

### Задача 3

Числа с ровно тремя нетривиальными делителями имеют вид $p^4$ (простое $p$), так как делители $p^4$ — это $1, p, p^2, p^3, p^4$, из которых нетривиальные: $p, p^2, p^3$. Перебираем простые $p$ в диапазоне $[\sqrt[4]{123456789},\, \sqrt[4]{223456789}]$, проверяем попадание $p^4$ в отрезок и считаем нетривиальные делители. Выводим само число и его наибольший нетривиальный делитель.

```python
def solve_ex3():
    def find_divs(number):
        divs = set()
        for i in range(2, int(number ** 0.5) + 1):
            if number % i == 0:
                divs.add(i)
                divs.add(number // i)
        return divs

    for number in range(int(123456789 ** 0.25), int(223456789 ** 0.25) + 1):
        if 123456789 <= number ** 4 <= 223456789:
            divs = find_divs(number ** 4)
            if len(divs) == 3:
                print(number ** 4, max(divs))
```

---

## Результаты

## Все

![all_ex](/lab3/2026-03-06_12-09.png)

### Задача 1

Количество допустимых пятизначных восьмеричных чисел:

```
504
```

### Задача 2

Количество цифр `8` в девятичной записи выражения $81^{17} + 3^{24} - 45$:

```
10
```

### Задача 3

Числа из $[123456789;\, 223456789]$ с ровно тремя нетривиальными делителями и их наибольший нетривиальный делитель:

```
131079601  1225043
141158161  1295029
163047361  1442897
```

---

## Список использованных источников

1. [Itertools в Python — Хабр](https://habr.com/ru/articles/itertools/)
2. [itertools — Functions creating iterators for efficient looping](https://docs.python.org/3/library/itertools.html)
3. [Итерируем правильно: 20 приёмов использования в Python модуля itertools](https://habr.com/ru/articles/205042/)