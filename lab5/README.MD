# Medium: Декоратор с опциональным параметром + поддержка рекурсии
Чтобы декоратор работал с рекурсивными функциями, мы можем:
Либо не декорировать саму рекурсивную функцию, а только её внешние вызовы.
Либо использовать подход с классом (см. ниже), где можно контролировать, сколько раз вызывать внешний вызов, игнорируя внутренние.
Но для простоты в Medium уровне достаточно показать, что декоратор принимает параметр и работает с обычными функциями.
Для демонстрации поддержки рекурсии — приведём пример без декорирования самой рекурсивной функции

