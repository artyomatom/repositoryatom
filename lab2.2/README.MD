# Отчет
## Задача 1

```
Тимофей составляет 5-буквенные коды из букв Т, И, М, О, Ф, Е, Й. 
Буква Й может использоваться в коде не более одного раза, при этом она не может стоять на первом месте, на последнем месте и рядом с буквой И. 
Все остальные буквы могут встречаться произвольное количество раз или не встречаться совсем. 
Сколько различных кодов может составить Тимофей?
```
Подключаем итертулс. Там есть много полезного для перебора вариантов.
Объявляем перфую функцию, которая вернет ответ для первой задачи def solve
Создаем переменную, из которой будем выделять по букве
Счетчик подходящих слов тоже как переменная
Далее
``` for combo in itertools.product(letters, repeat=5):```
Перебираем возможные комбинации из пяти букв
Это как 5 вложенных циклов
```word = ''.join(combo)```
Склеивает все буквы из кортежа
Далее считаем, сколько раз в слове встречается буква Й.
```if y_count > 1:
        continue```
Если больше одной, пропускаем это слово

Если одна, то дополнительные ограничения
```     if y_count == 1:
        idx = word.index('Й')
        if idx == 0 or idx == 4:
                continue```
Если Й стоит в начале или конце пропускаем слово

```if (idx > 0 and word[idx-1] == "И") or (idx < 4 and word[idx+1] == "И"):
                continue```
Условие если слева или справа от Й стоит буква И — слово тоже не подходит.
Проверяем: idx > 0 — чтобы не выйти за левую границу, idx < 4 — за правую.
valid_count += 1 
Увеличиваем счетчик, если все подходит
```return valid_count```

## Задача 2
Количество единиц в двоичной записи числа
Функция для второй задачи def solve_2():
Вычисляем число value = 4**2020 + 2**2017 - 15
```return bin(value).count('1')```
bin(value) — переводит число в строку двоичного вида, например: '0b101101...'
.count('1') — считает, сколько раз встречается цифра 1
Возвращаем это количество.

## Задача 3
Поиск чисел с ровно двумя делителями (в диапазоне)
Объявляем функцию для 3 ответа
```results = []```
Сюда складываем подходящие результаты
```for n in range(174457, 174506):```
Перебираем числа от 174457 до 174505
```divs = []```
Пустой список для делителей
```for d in range(2, int(n**0.5) + 1):```
Перебираем возможные делители от 2 до √n. Это оптимизация: если d делит n, то n//d тоже делитель
```if n % d == 0:```
Если d делит n без остатка — это делитель.
``` divs.append(d)```
Добавляем d в список делителей
```if d != n // d:
    divs.append(n // d)```
Если d и n//d — разные числа (то есть n не является полным квадратом), добавляем и второй делитель.
```results.sort(key=lambda x: x[0])```
Сортируем результаты по значению n (на самом деле, они и так идут по порядку, но на всякий случай).

